# Backend Environment Variables

# Database connection string (PostgreSQL with TimescaleDB)
DATABASE_URL=postgresql://ilog_user:changeme123@timescaledb:5432/ilog

# JWT secret for backend authentication (minimum 32 characters)
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Frontend URL for generating registration links
# Development: http://localhost:3000
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:3000

# Registration settings
# Set to true to allow public signups without a token
# Default: false (requires registration token for security)
# When false, only users with valid registration tokens can sign up
ALLOW_PUBLIC_SIGNUP=false
