# Frontend Environment Variables
# Copy this file to .env.local and update the values for your environment

# Backend API URL (for server-side requests and authentication)
# Development: http://localhost:8080
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8080

# WebSocket URL for real-time log streaming
# Development: ws://localhost:8080
# Production: wss://api.yourdomain.com
NEXT_PUBLIC_WS_URL=ws://localhost:8080

# Database connection string (PostgreSQL with TimescaleDB)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://ilog_user:changeme123@timescaledb:5432/ilog

# Better Auth Secret (minimum 32 characters)
# Generate a secure random string for production
BETTER_AUTH_SECRET=your-secret-key-change-in-production-min-32-chars

# Better Auth URL (frontend URL for authentication callbacks)
# Development: http://localhost:3000
# Production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# iLog Backend Endpoint for log ingestion (used by proxy.ts)
# Development: http://backend:8080/v1/logs
# Production: https://api.yourdomain.com/v1/logs
NEXT_PUBLIC_ILOG_ENDPOINT=http://backend:8080/v1/logs

# iLog HTTP Token for log ingestion
# Get this from your iLog backend service tokens
ILOG_HTTP_TOKEN=proj_xxx_yyy

# Service name for frontend logs
NEXT_PUBLIC_SERVICE_NAME=ilog-frontend
